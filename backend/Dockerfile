# Backend image. Build details are intentionally incomplete until Sprint 4 (deployment).
# TODO: choose a base image (python:3.11-slim vs. distroless) and pin the digest.
# TODO: multi-stage build -- builder installs deps, runtime copies site-packages.
# TODO: install system deps required by the PDF/DOCX parsers, if any.
# TODO: create a non-root user and run as it.
# TODO: COPY app/ and set WORKDIR.
# TODO: CMD uvicorn app.main:app --host $API_HOST --port $API_PORT
# TODO: HEALTHCHECK against GET /health.
