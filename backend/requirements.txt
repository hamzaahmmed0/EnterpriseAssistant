# Runtime dependencies.
# Lower bounds are set to versions whose APIs this code actually uses. Pin exact versions after
# the first successful install (`pip freeze`) and record them in docs/DECISIONS.md -- results are
# not reproducible otherwise.

# --- web layer ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9      # contract file uploads
pydantic>=2.6
pydantic-settings>=2.2

# --- agent layer ---
langgraph>=0.2               # the adaptive loop; adaptive.py falls back to a plain loop without it
langchain-core>=0.3

# --- llm + embeddings (ADR-005: both served by Ollama over HTTP) ---
httpx>=0.27

# --- tokenizer used only to measure chunk size (ADR-003) ---
tiktoken>=0.6

# --- vector store ---
qdrant-client>=1.9

# --- relational (ADR-008: create_all, no alembic) ---
sqlalchemy>=2.0
psycopg[binary]>=3.1

# --- document parsing ---
pypdf>=4.1                   # PDF text extraction
python-docx>=1.1             # DOCX text extraction

# --- mcp ---
mcp>=2.0                     # official Python MCP SDK (MCPServer; was FastMCP before 2.x)
