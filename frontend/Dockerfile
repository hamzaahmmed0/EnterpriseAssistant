# Frontend image. Build details are intentionally incomplete until Sprint 4 (deployment).
# TODO: pick a node base image and pin the digest.
# TODO: multi-stage build -- deps, build, runner.
# TODO: decide dev vs. standalone production output (see next.config.mjs).
# TODO: run as a non-root user.
# TODO: EXPOSE 3000 and CMD to start the server.
