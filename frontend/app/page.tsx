/**
 * Login page: pick one of the demo users and sign in.
 */

export default function LoginPage() {
  throw new Error("Not implemented");
}

// TODO:
//  1. Render the demo user selector plus a password field.
//  2. Submit through a server action calling lib/api.login, then setSession and redirect to /ask.
//  3. Show a generic failure message that does not reveal whether the user id exists.
//  4. List the demo users with their department and access level, so a viewer can see which
//     boundary each one sits on before switching.
